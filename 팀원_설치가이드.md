# 인조이(INJOY) 로컬 실행 가이드

Next.js가 처음이어도 아래 순서만 따라 하면 됨. Python의 `requirements.txt` 같은 별도 설치 파일은 없고, `frontend/package.json`에 필요한 패키지가 다 정의되어 있어서 명령어 한 번이면 끝.

## 0. 설치 아예 하기 싫으면 — GitHub Codespaces (무료, 브라우저에서 바로 실행)

로컬에 Node.js 설치조차 하기 싫으면 이 방법 써도 됨. GitHub 계정만 있으면 되고, 개인 계정 기준 월 60시간(2코어 머신)까지 무료라 하커톤 동안 쓰기엔 충분함. 로컬 컴퓨터엔 아무것도 안 깔림.

1. GitHub 저장소(`hyunie0130/IU_team9`) 페이지 접속
2. 초록색 `Code` 버튼 → `Codespaces` 탭 → `Create codespace on main`
3. 1~2분 대기하면 브라우저 안에 VS Code 화면이 뜸
4. 그 안의 터미널에 `./run_frontend.sh` 입력
5. 화면 아래에 "포트 3000 열림" 알림이 뜨면 그 링크 클릭 → 새 탭에서 앱 확인

main이 업데이트된 걸 다시 보고 싶으면 Codespace 터미널에서 `git pull` 한 번 하고 다시 `./run_frontend.sh` 실행하면 됨. 아래 1~6번은 로컬에 직접 설치해서 돌리고 싶은 사람만 보면 됨.

## 1. 사전 준비 — Node.js 설치

- 터미널에서 `node -v` 입력해서 버전 확인
- `22.13.0` 이상이어야 함. 없거나 버전이 낮으면 [nodejs.org](https://nodejs.org)에서 LTS(22.x 이상) 설치

**Windows에서 `npm : 'npm' 용어가 ... 인식되지 않습니다` 에러가 뜨면** → Node.js가 아예 설치가 안 된 상태. 아래 순서대로:

1. [nodejs.org](https://nodejs.org)에서 Windows용 설치파일(.msi, LTS) 다운로드해서 설치 — 마법사 그대로 다음/다음 (PATH 추가 옵션은 기본 체크되어 있음)
2. **설치 후 열려있던 PowerShell/터미널 창을 완전히 닫고 새로 열기** (이게 제일 중요 — 이미 켜져있던 창은 PATH 변경이 반영 안 돼서 새로 안 열면 설치해도 같은 에러가 계속 남)
3. 새 터미널에서 `node -v`, `npm -v`로 버전이 뜨는지 확인 후 2번 단계(실행)로 진행

## 2. 실행

```bash
cd IU_team9
./run_frontend.sh
```

최초 실행 시 `npm install`이 자동으로 돌아감 (몇 분 걸릴 수 있음). 이후 브라우저에서 `http://localhost:3000` 접속.

## 3. AI 답변 켜기 (선택, 안 해도 앱은 정상 작동)

`frontend/.env.local` 파일을 직접 만들고 아래처럼 팀 API 키를 넣기:

```
OPENAI_API_KEY=팀에서_받은_키
```

이 파일은 `.gitignore`에 걸려있어서 git에는 안 올라가— 그래서 각자 로컬에 직접 만들어야 함. 키는 깃허브가 아니라 카톡/슬랙 DM으로 받기. 키가 없어도 앱은 실행되고, FAQ 기반 답변까지는 정상 작동함 (실시간 GPT 답변만 안 나옴).

## 4. 코드 어디를 고치면 되나

| 하고 싶은 것 | 파일 |
|---|---|
| 홈/생활가이드/AI질문/내주변/마이라이프 화면 UI | `frontend/app/MainApp.tsx` |
| AI 질문 탭 답변 로직 (FAQ 검색 + GPT 호출) | `frontend/app/api/chat/route.ts` |
| 생활가이드 카테고리·체크리스트 데이터 | `frontend/app/data.ts` |
| 지도(내 주변) 관련 컴포넌트 | `frontend/app/MapView.tsx` |
| FAQ / 기관 연락처 / 지도 장소 데이터(JSON) | `frontend/public/faq.json`, `institution_contacts.json`, `nearby_places.json` |
| 다국어 번역 텍스트 | `frontend/app/app-translations.json`, `onboarding-i18n.ts` |

전체가 대부분 `MainApp.tsx` 한 파일에 몰려있는 구조라, 동시에 여러 명이 같은 부분을 건드리면 git 충돌이 날 수 있음 — 작업 전에 서로 어느 섹션(홈/가이드/질문/내주변/마이라이프) 건드리는지 한 번씩 얘기하고 시작하는 게 안전함.

## 5. 자주 쓰는 명령어 (`frontend/` 폴더 안에서)

- `npm run dev` — 로컬 개발 서버 실행
- `npm run lint` — 코드 스타일 체크
- `npm run build` — 배포용 빌드가 제대로 되는지 확인

## 6. 막힐 때

- `localhost:3000`이 안 열림 → 터미널에 에러 메시지가 있는지 확인, `node -v`로 버전 다시 확인
- `npm install` 도중 에러 → `frontend/node_modules` 폴더 지우고 다시: `rm -rf node_modules && npm install`
- AI 질문 탭에서 항상 똑같은 정형화된 답만 나옴 → `.env.local`에 키가 없는 상태 (정상 동작이고, FAQ 기반 폴백 답변이 나가는 중)
